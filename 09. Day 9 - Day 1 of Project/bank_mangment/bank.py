from Account import Account
from Customer import Customer
from Transaction import Transaction


if __name__ == '__main__':

    # print('Create customer..')
    # full = input('Enter full name:')
    # f_name, m_name, l_name = full.split(' ')
    # username = input('Enter ussername for you: ')
    # password = input('Enter password: ')
    # address = input('Enter your address: ')
    # birth_date = input('Enter your birth date: ')
    # phone = input('Enter your phone number: ')
    # email = input('Enter your email: ')
    # age = int(input('Enter your age: '))

    # cust = Customer(0, f_name, m_name, l_name, username, password, address, 
    #                 birth_date, phone, email, age)

    # acct = Account()
    tran = Transaction()